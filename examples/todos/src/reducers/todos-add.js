// A reducer implementation for the 'ADD_TODO' action
// for todo object defined by id, text and completed
const todos = (state = [], action) => {
  switch (action.type) {
    case 'ADD_TODO':
  }
}

export default todos
